# Base URL of the memory API. The default uses the Vite dev-server proxy
# (see vite.config.ts), which forwards /api/* to the local API.
# Set this to an absolute URL (e.g. http://localhost:3000) to bypass the proxy.
VITE_API_BASE_URL=/api
